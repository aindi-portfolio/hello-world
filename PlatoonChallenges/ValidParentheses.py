def isValid(s: str):
        s_list = list(s)
        return print(s_list)
        dict = {
                '(': ')',
                '[': ']',
                '{': '}'
        }


s = "()[]{}"
isValid(s)